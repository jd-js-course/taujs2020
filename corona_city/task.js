const task={
    title,
    color: '#653f5e',
    diabled: false,
constructor(title, color, diable){
    this.title = title,
    this.color = color,
    this.disbled = disable;
},
disble(){
    this.diabled = true;
}
}